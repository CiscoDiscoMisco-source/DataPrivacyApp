# Production environment variables for Vercel deployment
REACT_APP_API_URL=/api
NODE_ENV=production
# Add secrets like JWT_SECRET_KEY via Vercel environment variables UI
# rather than in this file for security 